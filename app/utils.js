export function Point(x, y) {
    this.x = x;
    this.y = y;
}

export function Pen(width, color) {
    this.width = width;
    this.color = color;
}

export function Font(size, name) {
    this.name = name;
    this.size = size;
}